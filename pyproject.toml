[tool.poetry]
name = "data-synthesizer"
version = "0.1.0"
description = ""
authors = ["yfhan <yifeicd@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
openai = "^0.28.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
